#include <stdio.h>
#include <string.h>

// Helper function to map Roman characters to integer values
int romanCharToInt(char c) {
    switch (c) {
        case 'I': return 1;
        case 'V': return 5;
        case 'X': return 10;
        case 'L': return 50;
        case 'C': return 100;
        case 'D': return 500;
        case 'M': return 1000;
        default:  return 0;
    }
}

int romanToInt(char *s) {
    int total = 0;
    int len = strlen(s);

    for (int i = 0; i < len; i++) {
        int current = romanCharToInt(s[i]);
        int next = (i + 1 < len) ? romanCharToInt(s[i + 1]) : 0;

        // Subtractive combination (e.g., IV = 5 - 1 = 4)
        if (current < next) {
            total -= current;
        } else {
            total += current;
        }
    }

    return total;
}
