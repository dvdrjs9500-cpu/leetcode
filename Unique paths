int uniquePaths(int m, int n) {
    long long ans = 1;
    int totalSteps = m + n - 2;
    int k = m - 1 < n - 1 ? m - 1 : n - 1;
    
    for (int i = 1; i <= k; i++) {
        ans = ans * (totalSteps - k + i) / i;
    }
    
    return (int)ans;
}
