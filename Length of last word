#include <stdio.h>
#include <string.h>

int lengthOfLastWord(char* s) {
    int length = 0;
    int i = strlen(s) - 1;

    // Phase 1: Skip trailing spaces at the end of the string
    while (i >= 0 && s[i] == ' ') {
        i--;
    }

    // Phase 2: Count characters of the last word
    while (i >= 0 && s[i] != ' ') {
        length++;
        i--;
    }

    return length;
}
